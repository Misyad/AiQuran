"use client"; import SurahPicker from "./SurahPicker"; import { getSurah } from "@/lib/quran/data"; type NavMode = "mushaf" | "presentation"; interface NavBarProps { surah: number; ayah: number; onSurahChange: (s: number) => void; onAyahChange: (a: number) => void; mode: NavMode; onModeChange?: (m: NavMode) => void; ayahCount: number; } export default function NavBar({ surah, ayah, onSurahChange, onAyahChange, mode, onModeChange, ayahCount, }: NavBarProps) { const surahInfo = getSurah(surah); return ( <nav className="flex items-center gap-3 px-4 py-2 border-b border-amber-200 bg-white z-30"> <a href="/" className="font-bold text-sm text-green-800 whitespace-nowrap"> QLVRS </a> <SurahPicker selected={surah} onSelect={onSurahChange} /> <select value={ayah} onChange={(e) => onAyahChange(Number(e.target.value))} className="px-2 py-1.5 text-sm rounded bg-white border border-gray-200 " > {Array.from({ length: ayahCount }, (_, i) => i + 1).map((n) => ( <option key={n} value={n}>Ayat {n}</option> ))} </select> <div className="flex-1" /> {surahInfo && ( <span className="text-xs text-muted-foreground hidden sm:block" dir="rtl"> {surahInfo.name_arabic} </span> )} {onModeChange && ( <div className="flex rounded border border-gray-200 overflow-hidden"> {(["mushaf", "presentation"] as const).map((m) => ( <button key={m} onClick={() => onModeChange(m)} className={`px-3 py-1 text-xs ${ mode === m ? "bg-amber-600 text-white" : "bg-white hover:bg-gray-50 :bg-gray-700" }`} > {m === "mushaf" ? "Mushaf" : "Tampilan"} </button> ))} </div> )} </nav> ); } 
