"use client"; import { ALL_SURAH } from "@/lib/quran/data"; import { useMemo, useState } from "react"; interface SurahPickerProps { selected?: number; onSelect: (surahId: number) => void; } export default function SurahPicker({ selected, onSelect }: SurahPickerProps) { const [search, setSearch] = useState(""); const [open, setOpen] = useState(false); const filtered = useMemo(() => { if (!search.trim()) return ALL_SURAH; const q = search.toLowerCase(); return ALL_SURAH.filter( (s) => s.name_transliteration.toLowerCase().includes(q) || s.name_translation.toLowerCase().includes(q) || s.name_arabic.includes(search) || String(s.id).includes(search) ); }, [search]); const selectedSurah = ALL_SURAH.find((s) => s.id === selected); return ( <div className="relative"> <button onClick={() => setOpen(!open)} className="flex items-center gap-2 px-3 py-1.5 text-sm rounded bg-white border border-gray-200 hover:bg-gray-50 :bg-gray-700 min-w-[180px]" > <span className="flex-1 text-left truncate"> {selectedSurah ? `${selectedSurah.id}. ${selectedSurah.name_transliteration}` : "Pilih Surah"} </span> <svg className="w-4 h-4 opacity-50" fill="none" stroke="currentColor" viewBox="0 0 24 24"> <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" /> </svg> </button> {open && ( <> <div className="fixed inset-0 z-10" onClick={() => setOpen(false)} /> <div className="absolute z-20 mt-1 w-72 bg-white border border-gray-200 rounded-lg shadow-xl max-h-80 overflow-hidden flex flex-col"> <div className="p-2 border-b border-gray-200 "> <input autoFocus placeholder="Cari surah..." value={search} onChange={(e) => setSearch(e.target.value)} className="w-full px-2 py-1 text-sm rounded bg-gray-50 border border-gray-200 outline-none focus:ring-1 focus:ring-amber-500" /> </div> <div className="overflow-y-auto flex-1"> {filtered.map((s) => ( <button key={s.id} onClick={() => { onSelect(s.id); setOpen(false); setSearch(""); }} className={`w-full text-left px-3 py-2 text-sm flex items-center gap-3 hover:bg-amber-50 :bg-amber-900/30 ${ s.id === selected ? "bg-amber-100 font-medium" : "" }`} > <span className="text-xs text-muted-foreground w-6 text-right"> {s.id} </span> <span className="flex-1">{s.name_transliteration}</span> <span className="text-xs text-muted-foreground">{s.name_translation}</span> </button> ))} </div> </div> </> )} </div> ); } 
